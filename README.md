# cava-raw-reader
This crate provides `CavaReader` to parse the output from cava in raw mode.
Also provides `CavaHandle` and `CavaConfig` to easily setup a configurable cava child process
for all your audio visualizing needs!

## Why?
It would be cooler to link to libcava instead of course, but [libcava-rs](https://codeberg.org/tranzystorekk/libcava-rs)
is unmaintained and im to lazy to fix it :3

## Usage
take a look at `examples` <3
